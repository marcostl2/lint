import a from "./test";

describe("testing", () => {
  it("do something", () => {
    expect(a).toBeGreaterThan(2);
    expect(10).toBeGreaterThan(2);
    console.log("iae2222");
    // --rule 'no-console: error'"
  });
});
